const sideBarImg = require('../assets/images/sideBar1.png');

const SideBar = () => {
  return (
    <>
      <img style={{}} src={sideBarImg} alt="SideBar bar"></img>
    </>
  );
};

export default SideBar;